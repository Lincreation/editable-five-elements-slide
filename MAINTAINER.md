# 制作・公開運用メモ

このリポジトリと既存GitHub Pagesは公開状態です。顧客名、契約内容、実データ、個人情報、社内URLを追加しません。

現行のPages workflowは`main`へのpush時にリポジトリ全体を公開対象にするため、今回の研修パッケージを公開する用途には使いません。公開する場合は、公開専用ディレクトリをallowlist方式で用意し、Pagesの対象をそのディレクトリだけに変更したうえで、明示承認後に実行します。

`*.inspect.ndjson`は制作時の検査ファイルで、commit・配布・公開の対象外です。
